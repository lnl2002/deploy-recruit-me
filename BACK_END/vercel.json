{
    "version": 2,
    "builds": [
        {
            "src": "./index.ts",
            "use": "@vercel/node",
            "config": {
                "distDir": "dist"
            }
        }
    ],
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/"
        }
    ]
}
